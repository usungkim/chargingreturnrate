# EV Infra 충전 복귀율 계산기 환경변수 템플릿
# 이 파일을 복사해서 .env 로 저장한 후 실제 값을 채우세요.
#   cp .env.example .env
#
# .env 는 .gitignore 에 의해 Git 추적에서 제외됩니다. 절대 커밋하지 마세요.

# PostgreSQL 접속 DSN
# 형식: postgresql+psycopg2://유저:비밀번호@호스트:포트/DB명
PG_DSN=postgresql+psycopg2://user:password@localhost:5432/dbname